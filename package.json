{
  "name": "bda_mongo",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "slidev build",
    "dev": "slidev --open",
    "export": "slidev export",
    "build:mongo": "slidev build courses/mongo/slides.md --out dist/mongo",
    "build:all": "npm run build:mongo",
    "deploy": "npm run build:all && npx gh-pages -d courses/mongo/dist"
  },
  "dependencies": {
    "@slidev/cli": "^52.2.5",
    "@slidev/theme-default": "latest",
    "@slidev/theme-seriph": "latest",
    "slidev-theme-neversink": "^0.4.0",
    "vue": "^3.5.22"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  }
}
